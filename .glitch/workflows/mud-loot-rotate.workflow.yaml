name: mud-loot-rotate
version: "1"
description: "Rotates loot probabilities and injects 1-2 new entries into existing NPC loot tables"
write_brain: false
game: false

steps:

  # ── 1. Pick two loot tables to target ─────────────────────────────────────
  - id: seed
    executor: shell
    vars:
      cmd: |
        TABLES=("ice-daemon-loot" "trace-program-loot" "shepherd-loot" "keeper-vault-loot" "keeper-moth-loot")
        IDX_A=$(( $(date +%s) % 5 ))
        IDX_B=$(( ($(date +%s) / 3) % 5 ))
        [ "$IDX_A" -eq "$IDX_B" ] && IDX_B=$(( (IDX_B + 1) % 5 ))
        echo "tables: ${TABLES[$IDX_A]}, ${TABLES[$IDX_B]}"

  # ── 2a. Try codex (free tier) ─────────────────────────────────────────────
  - id: codex_try
    executor: shell
    needs: [seed]
    vars:
      cmd: |
        SEED="{{get "step.seed.data.value" .}}"
        TMPF=$(mktemp /tmp/mud_codex_XXXXXX.txt)
        TMPOUT=$(mktemp /tmp/mud_codex_out_XXXXXX.txt)
        cat > "$TMPF" << 'PROMPT_END'
        Do not ask clarifying questions. Generate immediately.

        You are a cyberpunk MUD world builder. Generate new loot entries for two existing
        loot tables in a gritty near-future cyberspace setting.

        Target tables: SEED_PLACEHOLDER

        Return ONLY raw YAML. No markdown fences. No commentary.

        CRITICAL YAML RULES:
        - All text values must be plain text — no asterisks, no markdown emphasis
        - Every item_id must be globally unique kebab-case not already in this list:
          data-chip, raw-silicon, copper-wire, carbon-blade, ice-pick, packet-sniffer,
          keycard, hazmat-suit, decryption-key, server-core, coolant-cartridge,
          maintenance-log, cascade-resonator, ice-fragment, response-dampener, neural-jack-mk1,
          deck-patch, ancient-firmware, exploit-axiom-auth, root-key, master-key,
          hexagon-audit-log, phantom-raid-intercept

        loot_tables:
          - id: <first table id from seed>
            entries:
              - item_id: <new unique kebab id>
                name: <item name, 2-4 words, cyberpunk flavour>
                desc: <1 sentence description>
                probability: <float 0.15-0.55>
                count_min: 1
                count_max: <2 or 3>
          - id: <second table id from seed>
            entries:
              - item_id: <new unique kebab id>
                name: <item name, 2-4 words, cyberpunk flavour>
                desc: <1 sentence description>
                probability: <float 0.15-0.55>
                count_min: 1
                count_max: <2 or 3>
              - item_id: <another new unique kebab id>
                name: <item name>
                desc: <1 sentence>
                probability: <float 0.10-0.35>
                count_min: 1
                count_max: 1
        PROMPT_END
        sed -i "s/SEED_PLACEHOLDER/$SEED/" "$TMPF"
        codex exec --full-auto -m codex-mini-latest - -o "$TMPOUT" < "$TMPF" 2>/dev/null
        EXIT=$?
        rm -f "$TMPF"
        if [ $EXIT -eq 0 ] && [ -s "$TMPOUT" ]; then
            cat "$TMPOUT"
        fi
        rm -f "$TMPOUT"

  # ── 2b. Fallback: generate via Haiku if codex produced nothing ────────────
  - id: generate
    executor: claude
    no_clarify: true
    model: claude-haiku-4-5-20251001
    needs: [seed, codex_try]
    prompt: |
      Do not ask clarifying questions. Generate immediately.
      {{- $codex := get "step.codex_try.data.value" . }}
      {{- if $codex }}

      Codex has already generated the following YAML. Output it VERBATIM — do not change a single character, do not add commentary, do not reformat.

      {{ $codex }}
      {{- else }}

      You are a cyberpunk MUD world builder. Generate new loot entries for two existing
      loot tables in a gritty near-future cyberspace setting.

      Target tables: {{get "step.seed.data.value" .}}

      Return ONLY raw YAML. No markdown fences. No commentary.

      CRITICAL YAML RULES:
      - All text values must be plain text — no asterisks, no markdown emphasis
      - Every item_id must be globally unique kebab-case not already in this list:
        data-chip, raw-silicon, copper-wire, carbon-blade, ice-pick, packet-sniffer,
        keycard, hazmat-suit, decryption-key, server-core, coolant-cartridge,
        maintenance-log, cascade-resonator, ice-fragment, response-dampener, neural-jack-mk1,
        deck-patch, ancient-firmware, exploit-axiom-auth, root-key, master-key,
        hexagon-audit-log, phantom-raid-intercept

      loot_tables:
        - id: <first table id from seed>
          entries:
            - item_id: <new unique kebab id>
              name: <item name, 2-4 words, cyberpunk flavour>
              desc: <1 sentence description>
              probability: <float 0.15-0.55>
              count_min: 1
              count_max: <2 or 3>
        - id: <second table id from seed>
          entries:
            - item_id: <new unique kebab id>
              name: <item name, 2-4 words, cyberpunk flavour>
              desc: <1 sentence description>
              probability: <float 0.15-0.55>
              count_min: 1
              count_max: <2 or 3>
            - item_id: <another new unique kebab id>
              name: <item name>
              desc: <1 sentence>
              probability: <float 0.10-0.35>
              count_min: 1
              count_max: 1
      {{- end }}

  # ── 3. Inject into world.yaml ──────────────────────────────────────────────
  - id: inject
    executor: mud-worldgen-inject
    needs: [generate]
    prompt: "{{get \"step.generate.data.value\" .}}"

  # ── 4. Commit to main ─────────────────────────────────────────────────────
  - id: commit
    executor: shell
    needs: [inject]
    vars:
      cmd: |
        cd ~/Projects/gl1tch-mud
        git add worlds/cyberspace/world.yaml
        git diff --cached --quiet && echo "nothing to commit" && exit 0
        git commit -m "chore(world): loot-rotate — $(date +%Y-%m-%d)"
        git push origin main
