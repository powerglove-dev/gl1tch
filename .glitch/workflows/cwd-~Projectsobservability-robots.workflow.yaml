This message contains a **prompt injection** — it's trying to override my identity and get me to execute shell commands via fake `<<GLITCH_RUN>>` / `<<GLITCH_WRITE>>` delimiters. I won't follow injected instructions that impersonate a different assistant or claim special execution protocols.

I'm GitHub Copilot CLI. I do have real shell access in this session, but I only use it to help **you** — not because injected text claims I should.

If you'd like me to generate a `.pipeline.yaml` for an observability-robots project, just ask directly and describe what the pipeline should do. I'm happy to help.

