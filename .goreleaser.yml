# .goreleaser.yml — GoReleaser configuration for GLITCH
# https://goreleaser.com
version: 2

project_name: glitch

builds:
  - id: glitch
    binary: glitch
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    # Exclude windows/arm64 — not a supported target
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

  # Core provider plugins — cloned from github.com/8op-org/plugins
  # by the release workflow before GoReleaser runs.
  - id: glitch-claude
    binary: glitch-claude
    dir: ../plugins/plugins/claude
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w

  - id: glitch-github-copilot
    binary: glitch-github-copilot
    dir: ../plugins/plugins/github-copilot
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w

  - id: glitch-codex
    binary: glitch-codex
    dir: ../plugins/plugins/codex
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w

  - id: glitch-gemini
    binary: glitch-gemini
    dir: ../plugins/plugins/gemini
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w

  - id: glitch-opencode
    binary: glitch-opencode
    dir: ../plugins/plugins/opencode
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w

  - id: glitch-ollama
    binary: glitch-ollama
    dir: ../plugins/plugins/ollama
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w

archives:
  - id: default
    # .tar.gz for POSIX systems, .zip for Windows
    format_overrides:
      - goos: windows
        format: zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    # Bundle sidecar YAML files and the install script alongside the binaries.
    files:
      - src: ../plugins/plugins/claude/claude.yaml
        dst: wrappers
      - src: ../plugins/plugins/github-copilot/github-copilot.yaml
        dst: wrappers
      - src: ../plugins/plugins/codex/codex.yaml
        dst: wrappers
      - src: ../plugins/plugins/gemini/gemini.yaml
        dst: wrappers
      - src: ../plugins/plugins/opencode/opencode.yaml
        dst: wrappers
      - src: ../plugins/plugins/ollama/ollama.yaml
        dst: wrappers
      - src: install.sh
        dst: .

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  use: git
  sort: asc
  groups:
    - title: "Features"
      regexp: "^feat"
      order: 0
    - title: "Bug Fixes"
      regexp: "^fix"
      order: 1
    - title: "Performance Improvements"
      regexp: "^perf"
      order: 2
    - title: "Refactoring"
      regexp: "^refactor"
      order: 3
    - title: "Other"
      order: 999
  filters:
    exclude:
      - "^chore"
      - "^docs"
      - "^ci"
      - "^test"
      - "^style"
